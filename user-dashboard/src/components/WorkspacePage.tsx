import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const WorkspacePage: React.FC = () => {
  return (
    <div>
      <Title>Workspace</Title>
      <p>This is a placeholder for the workspace page.</p>
    </div>
  );
};

export default WorkspacePage;
