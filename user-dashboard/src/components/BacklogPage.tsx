import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const BacklogPage: React.FC = () => {
  return (
    <div>
      <Title>Backlog</Title>
      <p>This is a placeholder for the backlog page.</p>
    </div>
  );
};

export default BacklogPage;
